const Arrow =( x, n) => {     //Используется функция ArrowFunction
    console.log ( x ** n);      // возвращает x в степени n
}

Arrow (7, 4);   // первая цифра означает возвышение. Вторая цифра является степень
